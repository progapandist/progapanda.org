{
  "scripts": {
    "start": "vite src",
    "build": "vite build src --outDir ../dist --emptyOutDir"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0"
  }
}
